# Simple command file to read a DateValue file, which contains all the
# monthly streamflow time series from HydroBase for Water District 38
# (Roaring Fork).  After reading the time series are read in, they can be viewed.
ReadDateValue(InputFile="RoaringFork-Streamflow-Month.dv")
